import { PLUS, MINUS, MUTIPLY, DIVIDE, RESULT } from './actionTypes';

export const plus = () => ({ type: PLUS });
export const minus = () => ({ type: MINUS });
export const multiply = () => ({ type: MUTIPLY });
export const divide = () => ({ type: DIVIDE });
export const result = () => ({ type: RESULT });
